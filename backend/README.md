# crypto-backend

FastAPI backend for the Crypto trading workstation. Managed with `uv`.

```bash
uv run uvicorn app.main:app --reload   # dev server on :8000
uv run pytest                          # tests
```
